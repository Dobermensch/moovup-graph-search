# Moovup - Question 1

Getting all paths to a point in graph and finding smallest number of hops to that point.

## installation

1. `git clone` repo
2. `cd` in directory
3. `npm i` or `yarn`
4. `yarn start`
